/**
 * Optional: set your deployed backend URL so the app works from GitHub Pages.
 * Leave empty to use local server (http://localhost:3000).
 * Example: window.REMOVE_BG_SERVER = "https://your-app.railway.app";
 */
window.REMOVE_BG_SERVER = "";
